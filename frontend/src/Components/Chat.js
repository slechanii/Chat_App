import React, { Component } from 'react'
import { Grid, GridRow, GridColumn } from 'semantic-ui-react';

export default class Chat extends Component {
    render() {
        return (
            <GridColumn className="workspace-chat" width={14}>
            Chat
            </GridColumn>
        )
    }
}
